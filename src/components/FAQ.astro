---
import { faqItems } from '../data/services';
---

<section class="section-padding bg-white" id="faq">
  <div class="container-custom max-w-4xl">
    <div class="animate-on-scroll text-center mb-10">
      <span class="inline-flex items-center gap-2 px-4 py-1.5 bg-primary-50 text-primary-700 rounded-full text-sm font-medium mb-4 border border-primary-200">FAQ</span>
      <h2 class="text-3xl md:text-4xl font-bold text-[#1A2B4A] mb-3">Questions fréquentes</h2>
      <p class="text-slate-600">Retrouvez les réponses aux questions les plus posées par nos clients.</p>
    </div>
    <div class="space-y-3">
      {faqItems.map((item, index) => (
        <div class:list={['animate-on-scroll', `delay-${Math.min((index + 1) * 100, 500)}`]}>
          <details class="group card-dark rounded-xl transition-colors">
            <summary class="flex items-center justify-between gap-4 cursor-pointer px-5 py-4 text-left">
              <h3 class="font-medium text-slate-800 group-open:text-primary-600 transition-colors">{item.question}</h3>
              <svg class="w-5 h-5 shrink-0 text-slate-400 group-open:text-primary-500 group-open:rotate-180 transition-all duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" /></svg>
            </summary>
            <div class="px-5 pb-4 text-sm text-slate-600 leading-relaxed">{item.answer}</div>
          </details>
        </div>
      ))}
    </div>
  </div>
</section>

<script type="application/ld+json" set:html={JSON.stringify({
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": faqItems.map(item => ({
    "@type": "Question",
    "name": item.question,
    "acceptedAnswer": { "@type": "Answer", "text": item.answer }
  }))
})} />
